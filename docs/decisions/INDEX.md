# Decision Index

One line per architectural decision, in number order. Each links to the full
record in this directory — read a record for its context, rationale, and
alternatives; read this index to know a decision exists and how it landed.

- [0001](0001-reuse-app-components-for-html-export.md) — The single-comparison HTML export reuses the app's React components via SSR + hydration rather than a standalone template, trading coupling for guaranteed formatting parity (Accepted)
